# Macro placement for the hardened OpenRAM SRAM.
# Format:  <instance_name> <x_um> <y_um> <orientation>
# The instance name is the SRAM instantiation inside rtl/sram_pim_macro.v (u_sram).
# Adjust x/y so the macro fits inside DIE_AREA with room for the standard-cell
# logic and the I/O pin ring (tune to the macro's actual LEF dimensions).
u_sram 60 60 N
